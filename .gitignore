# --- Python 相关 ---
__pycache__/  # Python 字节码缓存
*.py[cod]     # 编译后的 Python 文件（.pyc, .pyo, .pyd）
*.egg-info/    # setuptools 生成的包信息
dist/          # 打包后的分发文件
build/         # 编译生成的临时文件
.pytest_cache/ # pytest 缓存
venv/          # 虚拟环境目录（推荐用全名，避免误删）
env/           # 其他常见虚拟环境目录
*.env          # 环境变量文件（通常含敏感信息）

# --- PyCharm IDE 配置 ---
/.idea/         # PyCharm 项目配置目录
*.iml          # PyCharm 模块文件
*.ipr          # PyCharm 项目文件
*.iws          # PyCharm 工作区文件

# --- 操作系统/编辑器临时文件 ---
.DS_Store      # macOS 属性文件
Thumbs.db      # Windows 缩略图缓存
*.swp          # Vim 临时文件
*.swo          # Vim 临时文件

# --- 日志和数据库 ---
*.log          # 日志文件
*.sqlite       # SQLite 数据库文件
*.db           # 其他数据库文件

# --- 自定义忽略项 ---
/test/saves/
/.vscode/